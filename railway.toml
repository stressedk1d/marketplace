# Конфиг на уровне репозитория: сборка API из backend/Dockerfile
# (если Root Directory в UI не срабатывает)

[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
